10 REM Computes the sum and average of the numbers read from a user-specified 
20 REM text file (which might have been created via generateRandomNumberFile(4)). 
30 REM  
40 REM This program is part of an arrangement used to test group code export (issue 
50 REM #828) with FileAPI dependency. 
60 REM The input check loop has been disabled (replaced by a simple unchecked input 
70 REM instruction) in order to test the effect of indirect FileAPI dependency (only the 
80 REM called subroutine directly requires FileAPI now). 
90 REM Generated by Structorizer 3.32-36 
100 
110 REM Copyright (C) 2020-03-21 Kay Gürtzig 
120 REM License: GPLv3-link 
130 REM GNU General Public License (V 3) 
140 REM https://www.gnu.org/licenses/gpl.html 
150 REM http://www.gnu.de/documents/gpl.de.html 
160 
170 REM  
180 REM program ComputeSum
190 REM TODO: add the respective type suffixes to your variable names if required 
200 DIM values() AS ???
210 REM  
220 LET fileNo = 1000
230 REM Disable this if you enable the loop below! 
240 PRINT "Name/path of the number file"; : INPUT file_name
250 REM If you enable this loop, then the preceding input instruction is to be disabled 
260 REM and the fileClose instruction in the alternative below is to be enabled. 
270 REM DO 
280 REM   PRINT "Name/path of the number file"; : INPUT file_name 
290 REM   LET fileNo = fileOpen(file_name) 
300 REM LOOP UNTIL fileNo > 0 OR file_name = "" 
310 IF fileNo > 0 THEN
320   REM This should be enabled if the input check loop above gets enabled. 
330 REM   fileClose(fileNo) 
340   LET nValues = 0
350   ON ERROR GOTO 390
360   LET nValues = readNumbers(file_name, values, 1000)
370   GOTO 430
380   REM Start of error handler, FIXME: variable 'failure' should conatain error info ... 
390     PRINT failure
400     REM FIXME: The following exit code was intended to pass: -7 
410     STOP
420   REM End of error handler, resume here ... 
430   ON ERROR GOTO 0
440   LET sum = 0.0
450   FOR k = 0 TO nValues-1
460     LET sum = sum + values(k)
470   NEXT k
480   PRINT "sum = "; sum
490   PRINT "average = "; sum / nValues
500 END IF
510 END
520 REM  
530 REM Tries to read as many integer values as possible upto maxNumbers 
540 REM from file fileName into the given array numbers. 
550 REM Returns the number of the actually read numbers. May cause an exception. 
560 REM TODO: Add type-specific suffixes where necessary! 
570 FUNCTION readNumbers(fileName AS String, numbers AS integer(50), maxNumbers AS integer) AS integer
580   REM TODO: add the respective type suffixes to your variable names if required 
590   REM  
600   REM  
610   LET nNumbers = 0
620   LET fileNo = fileOpen(fileName)
630   IF fileNo <= 0 THEN
640     REM FIXME: Only a number is allowed as parameter: 
650     ERROR "File could not be opened!"
660   END IF
670   ON ERROR GOTO 750
680   DO WHILE NOT fileEOF(fileNo) AND nNumbers < maxNumbers
690     LET number = fileReadInt(fileNo)
700     LET numbers(nNumbers) = number
710     LET nNumbers = nNumbers + 1
720   LOOP
730   GOTO 780
740   REM Start of error handler, FIXME: variable 'error' should conatain error info ... 
750     REM FIXME: Only a number is allowed as parameter: 
760     ERROR 
770   REM End of error handler, resume here ... 
780   ON ERROR GOTO 0
790   fileClose(fileNo)
800   RETURN nNumbers
810 END FUNCTION
